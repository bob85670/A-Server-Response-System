# A-Server-Response-System
This is a web server response system for processing responses in JSON format such that the Android apps will be able to get data from the SQL database using Python &amp; SQLite.
